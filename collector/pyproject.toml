[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "tongyuan-collector"
version = "0.1.0"
description = "Local collector for TongYuan work knowledge ingestion."
requires-python = ">=3.13"

[tool.setuptools.packages.find]
where = ["src"]
